{
  "name": "crypto-saves",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ crypto-saves",
    "create-local": "graph create --node http://localhost:8020/ crypto-saves",
    "remove-local": "graph remove --node http://localhost:8020/ crypto-saves",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crypto-saves",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.2",
    "@graphprotocol/graph-ts": "^0.33.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
